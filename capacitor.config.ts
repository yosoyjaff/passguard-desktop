import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.passguard.starter',
  appName: 'PassGuard+',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
